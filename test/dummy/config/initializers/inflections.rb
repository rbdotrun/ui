# Intentionally empty — the engine uses `Ui` (not `UI`) as the camelized
# form of `ui`, so we do NOT want an `acronym "UI"` rule. Same for
# `RbrunUi`: the gem is namespaced as `RbrunUi`, not `RbrunUI`.
